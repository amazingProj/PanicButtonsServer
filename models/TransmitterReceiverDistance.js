function TransmitterReceiverDistance(frequency, RSSI){
    var CLEAR_DISTRACTION_ENVIRONMENT = 2
    function getParameterN(){
        return CLEAR_DISTRACTION_ENVIRONMENT
    }
    if (frequency == undefined || RSSI == undefined){
        return
    }
}